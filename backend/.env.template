# Email Configuration
# For Gmail, you need to:
# 1. Enable 2-factor authentication
# 2. Generate an app password
# 3. Use the app password here (not your regular password)

MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password
MAIL_DEFAULT_SENDER=Rent & Billing System <your-email@gmail.com>

# Other Configuration
SECRET_KEY=your-secret-key-here-change-this-in-production
DATABASE_URL=sqlite:///billing.db

# Instructions:
# 1. Copy this file to .env (without .template extension)
# 2. Update the email settings with your actual Gmail credentials
# 3. Generate app password from Google Account settings > Security > App passwords
# 4. Change the SECRET_KEY to a random string in production
